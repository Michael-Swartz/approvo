# Run this example against a checkout of approvo itself:
#   pip install -e ../..
# Or, once approvo is published: pip install approvo
fastapi>=0.111
uvicorn[standard]>=0.30
motor>=3.4
pydantic>=2.6
httpx>=0.27
